export interface ISubmission {
  id: string;
  statusDisplay: string;
  lang: string;
  runtime: string;
  timestamp: string;
  url: string;
  isPending: string;
  memory: string;
  absoluteUrl: string;
}
